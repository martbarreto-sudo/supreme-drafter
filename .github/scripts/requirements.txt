# Dependências do orquestrador de peer-review NEXUM TIER 0.
# Pinos mínimos validados para Python 3.12 (runner ubuntu-latest).
anthropic>=0.40
google-genai>=0.5
pdfplumber>=0.11
pytest>=8.0
pytest-cov>=5.0
